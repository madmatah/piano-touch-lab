import { MainLayout } from '../MainLayout';
import { FileDown } from 'lucide-react';

export const BackupPage = () => {
  return (
    <MainLayout pageTitle="Backup" pageIcon={<FileDown />}>
      <span></span>
    </MainLayout>
  );
};
